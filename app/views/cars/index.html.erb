<div class="container">
  <div class="row row-cols-4">
  <% @cars.each do |car|%>
    <% if user_signed_in?%>
    <% end %>
      <div class="col">  <% if car.images.attached?  %>
      <% car.images.each do |image| %>
      <%= image_tag image %>
      <h4> Brand - <%= car.brand %> </h4>
  <h4> Model - <%= car.model %> </h4>
  <h4> Price - <%= car.price %> </h4>
  <%= link_to 'like', likes_path(car_id: car.id), :method => :post %>
  <% if @like.present? %>
  <%= link_to 'dislike', like_path(car.id, @like.id), :method => :delete %>
  <%end%><br>
  Likes :<%= car.likes.count %><br><br>
  <%= link_to 'Show Car', car_path(car) %> 
        <% end %> 
      <% end %> </div>
    <% end %>
  </div>
</div>
